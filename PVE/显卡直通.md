# PVE 显卡直通(PCIe Passthrough)通用指南

> 通用性:与客户机系统无关(Windows / Linux 发行版均适用)
> 实测环境:PVE 9.2 + RX 6650 XT(03:00.0 显卡 + 03:00.1 音频)
> 关联:USB 设备直通见 [Xbox直通.md](Xbox直通.md),整盘直通见 [硬盘直通.md](硬盘直通.md)

---

## 1. 原理与前提

- 显卡通过 **VFIO** 从宿主剥离、整体交给虚拟机独占
- 虚拟机必须 **q35 机型**(PCIe 直通必需),Windows 11 另需 OVMF(UEFI)+ TPM(见 Windows 部署指南)
- `x-vga=1` + 显示置 none 时,显卡成为虚拟机唯一显示输出,画面走物理接口(HDMI/DP)

## 2. 宿主机一次性准备

```bash
# 1) BIOS:开启 VT-d、Above 4G Decoding(8GB+ 显存卡必需);可选 ErP

# 2) 内核参数(/etc/default/grub → update-grub → 重启)
#    GRUB_CMDLINE_LINUX_DEFAULT 追加:
#    intel_iommu=on iommu=pt initcall_blacklists=sysfb_init nomodeset  (Intel 示例)

# 3) 查设备实际 ID(以实测为准,别抄示例)
lspci -nn | grep -iE 'VGA|Audio'

# 4) VFIO 绑定(显卡 ID + 音频 ID 一起)
echo "options vfio-pci ids=1002:73ef,1002:ab28" > /etc/modprobe.d/vfio.conf
echo -e "vfio\nvfio_iommu_type1\nvfio_pci" >> /etc/modules
update-initramfs -u -k all && reboot

# 5) 验证
dmesg | grep -i -e DMAR -e IOMMU | head -3      # IOMMU enabled
ls /sys/kernel/iommu_groups/ | wc -l
lspci -nnk -s 03:00.0                             # Kernel driver in use: vfio-pci
```

## 3. IOMMU 分组核对

```bash
ls -l /sys/kernel/iommu_groups/*/devices/ | grep '03:00'
```

- **同组设备必须整体直通**(否则无法启动或设备异常)
- 显卡 + 音频同卡常分属相邻独立组(如 10/11)——各自单设备时**可分别直通**,互不牵连
- 组内混入宿主设备(网卡等)时需 `pcie_acs_override=downstream`(不推荐,先换插槽)

## 4. 挂载到虚拟机(关机状态)

```bash
# 显卡作为主显示(x-vga=1)直通
qm set <vmid> -hostpci0 0000:03:00.0,pcie=1,x-vga=1
# 同卡 HDMI/DP 音频功能单独一条
qm set <vmid> -hostpci1 0000:03:00.1,pcie=1
# 画面完全交给直通卡
qm set <vmid> -vga none
```

GUI 等价:硬件 → 添加 → PCI 设备(勾选 PCI-Express / Primary GPU / All Functions)。**⚠️ GUI 编辑框没有「启用」勾选项**,禁用设备 = 移除/重新添加。

## 5. 客机内驱动与点亮顺序

- **推荐先装驱动再挂卡**:先用标准 VGA 完成系统与驱动安装 → 关机挂卡 → 首次点亮成功率最高
- Windows:装厂商驱动(AMD Adrenalin 等);Linux:amdgpu/nvidia 内核自带
- 挂卡后 **PVE 控制台(noVNC)黑屏 = 正常**,画面在物理输出上

## 6. 回退维护法(需要 noVNC/虚拟显示时)

```bash
# 回退(等效"临时禁用显卡")
qm stop <vmid> && qm set <vmid> -delete hostpci0 && qm set <vmid> -vga std && qm start <vmid>
# 恢复
qm stop <vmid> && qm set <vmid> -hostpci0 0000:03:00.0,pcie=1,x-vga=1 && qm set <vmid> -vga none && qm start <vmid>
```

仅摘显卡保留音频时,QEMU 报 `Cannot reset device ... depends on group N` 警告——**无害,忽略**(复位依赖同卡另一功能,不阻塞运行)。

## 7. 排错速查

| 现象 | 处理 |
| --- | --- |
| 直通后无画面 | 线插在直通卡物理口(非核显口);电视/显示器输入源;先装驱动再挂卡(§5) |
| 需要回控制台 | 回退法(§6) |
| Windows 显卡 Code 43 | 驱动重装;宿主整机重启一次清复位状态;确认 Above 4G 已开 |
| 二次启动报 vfio reset 失败 | 警告无害;设备异常时宿主重启 |
| 核显/宿主显示异常 | 无头使用,参数中 nomodeset 保留即可 |
| IOMMU 组混杂无法分离 | 换 PCIe 插槽优先;万不得已才试 acs 补丁 |

## 8. 命令速查

```bash
lspci -nnk | grep -A3 -iE 'VGA|Display'      # 确认 vfio-pci 接管
qm config <vmid> | grep hostpci              # 确认挂载
qm stop <vmid> && qm set <vmid> -hostpci0 <地址>,pcie=1,x-vga=1 && qm start <vmid>
```

## 关联文档

- [Windows虚拟机部署/Windows10-11虚拟机部署指南.md](Windows虚拟机部署/Windows10-11虚拟机部署指南.md) — 完整 Windows 部署流程
- [硬盘直通.md](硬盘直通.md) / [Xbox直通.md](Xbox直通.md)
- [Bazzite直通硬盘Steam库.md](Bazzite直通硬盘Steam库.md) — 直通后客户机内使用案例
