# PVE 硬盘直通(整盘直通 / 控制器直通)通用指南

> 通用性:与客户机系统无关(Windows / Linux 均可)
> **脚本化**:整盘挂载/移除可用 [scripts/attach-disk.sh](scripts/attach-disk.sh)(attach|detach)
> 实测环境:PVE 9.2 + WD 1TB 机械盘整盘直通给 Win11 虚拟机
> 关联:显卡直通见 [显卡直通.md](显卡直通.md),USB 直通见 [Xbox直通.md](Xbox直通.md)

---

## 1. 两种方式与选型

| 方式 | 适用 | 注意 |
| --- | --- | --- |
| **整盘直通(推荐,常用)** | 数据盘/游戏库盘,宿主不再使用该盘 | 盘以块设备形式直接交给 VM,性能接近原生 |
| SATA 控制器直通 | 盘挂独立控制器(PCIe 扩展卡/HBA)时 | 控制器**不能同时挂宿主系统盘**;一旦直通宿主即失去该控制器全部盘 |

## 2. 整盘直通步骤

```bash
# 1) 识别目标盘(按型号/容量核对,别选错)
ls -l /dev/disk/by-id/ | grep -v -E 'part|nvme'
lsblk

# 2) 确认盘未被宿主使用(无挂载点、不在宿主存储里)
lsblk -o NAME,MOUNTPOINTS /dev/disk/by-id/<盘>     # MOUNTPOINTS 应为空

# 3) 挂给虚拟机(VM 关机状态)
qm set <vmid> -scsi1 /dev/disk/by-id/ata-XXXX       # scsi1 = 第二个 SCSI 盘

# 4) 验证
qm config <vmid> | grep scsi1
```

- 用 **by-id 路径**而非 /dev/sdX(盘符会漂移)
- scsiN 编号按序递增,不影响系统盘引导(boot 顺序不含它即可)

## 3. SATA 控制器直通(备选)

```bash
lspci -nnk | grep -A3 -i 'SATA'        # 确认控制器地址
qm set <vmid> -hostpci0 <控制器地址>,pcie=1
```

前提:该控制器只接数据盘、不接宿主系统/引导盘;直通后其下所有盘归 VM。

## 4. 客户机侧使用

**Windows**:
1. `Win+X` → 磁盘管理 → 新盘若"脱机"先右键**联机**;未初始化 → **初始化 GPT**
2. 未分配空间 → 新建简单卷 → **NTFS** → 分配盘符
3. 若磁盘管理里连盘都不显示 → VM 冷启动一次(重启无效的场合)

**Linux**:
```bash
lsblk                    # 盘以 /dev/vdb 或 sdX 出现
sudo mkfs.ext4 /dev/vdb  # 视需要格式化
sudo mount /dev/vdb /mnt/data
```

## 5. ⚠️ 安全要点

1. **数据清空发生在客户机内格式化那一步**——宿主侧操作全程不触碰数据
2. **系统安装/重装时选盘认容量**:直通盘(无分区表)在 Windows 安装器里显示为"未分配",容量明显大于虚拟盘——**别把系统装上去**(区分:虚拟盘 128G vs 直通盘实际容量)
3. 直通盘**宿主不要再挂载/使用**(双写风险)
4. 需回收盘时:VM 关机 → `qm set <vmid> -delete scsi1` → 盘回到宿主可用

## 6. 常见问题

| 现象 | 处理 |
| --- | --- |
| 客户机看不到盘 | VM 冷启动(非重启);确认 scsi1 在 conf;确认盘未被宿主占用 |
| Windows 显示"脱机" | 磁盘管理 → 右键 → 联机 |
| Windows 显示未初始化 | GPT 初始化后新建卷(见 §4) |
| 盘符每次变化 | 磁盘管理里固定盘符 |
| 直通 NVMe/SSD 的 TRIM | 直通盘上 Windows 自动处理;整盘直通不影响 |

## 7. 命令速查

```bash
# 挂载 / 卸载直通盘
qm set <vmid> -scsi1 /dev/disk/by-id/<盘>
qm set <vmid> -delete scsi1

# 控制器直通
qm set <vmid> -hostpci0 <控制器地址>,pcie=1
```

## 关联文档

- [Windows虚拟机部署/Windows10-11虚拟机部署指南.md](Windows虚拟机部署/Windows10-11虚拟机部署指南.md) — 完整 Windows 部署流程
- [显卡直通.md](显卡直通.md) / [Xbox直通.md](Xbox直通.md)
- [Bazzite虚拟机部署/Steam硬盘库.md](Bazzite虚拟机部署/Steam硬盘库.md) — Linux 客户机内挂载与使用案例
